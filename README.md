# StudentRecordManagementAPI
A RESTful API built with Node.js and Express to perform basic CRUD operations on student records. The application uses a JSON file as a lightweight data store to simulate database operations, making it suitable for learning backend fundamentals and REST API design.
